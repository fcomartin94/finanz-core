# Render Blueprint - Finanz API
# Conecta tu repo en https://dashboard.render.com y Render detectará este archivo

services:
  - type: web
    runtime: docker
    name: finanz-api
    region: oregon

    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: cloud

    healthCheckPath: /api/saldo
